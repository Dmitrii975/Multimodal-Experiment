## Экспериментальная мультимодальная система, основанная на текстовом якоре  
**Ну или же — «Text is all you need» :)**

---

Приветствую тебя, читатель! Ты наткнулся на репозиторий моего research-проекта, в котором я создаю систему, позволяющую приводить *любые типы данных* к единому векторному пространству.

---

### 🎯 **Какова цель проекта?**

1. Получить «дешёвую и доступную мультимодальность» и избавиться от необходимости явного создания «мостов» между модальностями.  
2. Найти способ быстро и качественно приводить мультимодальные данные к единому векторному пространству.  
3. Разработать универсальную системную архитектуру, позволяющую в кратчайшие сроки проводить исследования, проверять гипотезы и расширять набор поддерживаемых модальностей.

---

### ✅ **Текущий прогресс:**

1. Создана система из п. 2. Внедрение новой модальности занимает меньше времени.  
2. Поддерживается 4 модальности: текст (якорь), изображения, аудио, видео (косвено).  
3. Возможен кросс-модальный поиск.

#### MNIST + AUDIO MNIST dataset

>Узнать подробную информацию об обучении и посмотреть визуальные тесты вы можете в **ReportMNIST.ipynb**

1. Средняя косинусная схожесть между X и Y ≈ 0.99. Все 10 кластеров визуально хорошо различимы при визуализации через t-SNE.  
2. Прямых пар «изображение–аудио» в обучающем наборе не было, но модель смогла самостоятельно построить «мост» и связать их через текст.

#### Выборка из датасета Kinetics 400
[Ссылка на датасет](https://www.kaggle.com/datasets/rohanmallick/kinetics-train-5per?select=kinetics400_5per)

>Узнать подробную информацию о предобратоке данных, обучении и посмотреть визуальные тесты вы можете в **Kinetics.ipynb**


В датасете содержалось примерно по 50 видео на каждый из 400 классов, каждое видео было разбито на 3 модальности
* Изображение (1й кадр)
* Аудио (звуковая дорожка)
* Текст (название действия в видео)


#### Проведенные тесты:
|Тесты (проведены на том же датасете, что и при обучении) | Значение|
|:---:| :--: |
|Средняя косинусная схожесть между положительными примерами | ~0.9 для изображений и ~0.85 для аудио |
|Обучение LogisticRegression 80/20 и замер accuracy на test (проверка информативности эмбеддингов) | ~0.28 для изображений и аудио |
|Обучение LogisticRegression 20/80 и замер accuracy на test (проверка на пригодность к, относительно, автономной черновой разметке данных) | ~0.17 для изображений и аудио |

Как мы видим, результат не то чтобы очень впечатляет, учитывая то, что тесты проведены на эмбеддингах объектов, которые модель ранее видела. Возможная проблема - количество/качество данных и/или не найдена более качественная архитектура.

>*Примечание: модель, обученная на аудио требовала большего количества эпох на обучение и всегда показывала результат хуже. Скорее всего, проблема в нерепрезентативности аудиозаписей или в некачественных признаках, извлекаемых ResNet18*

---

### ⚙️ **Подход и почему именно текст**

**Главная идея** — текстом можно описать практически любые данные тем или иным способом. Помимо этого, текстовых данных очень много. По этим причинам у меня появилась идея связать каждую модальность с текстом с помощью небольшого «проектора», задача которого — перевести эмбеддинг некой модальности в пространство текстовых эмбеддингов.

Получается pipeline (пример для изображений):

> ResNet18 → Embedding → Projector → (Text-like Embedding)

Да, идея довольно проста и наивна, но у неё есть свои плюсы:

1. Обучение крайне дешёвое и быстрое (для работы использовалась только NVIDIA GTX 1050 Ti, 4 ГБ).
2. Универсальность платформы: можно менять текстовую модель в зависимости от доступной вычислительной мощности и требуемого качества; также можно подбирать якоря под специфические задачи.    
3. Дешёвая разметка: для модели необходимы только пары «объект – текстовый якорь». Все остальные межмодальные связи формируются косвенно.

---

#### ❓**Почему именно средняя косинусная схожесть и почему только для положительных примеров**

>Средняя косинусная схожесть была выбрана интуитивно - нам требуется их максимальная "смысловая похожесть", но при обучении использовал MSE (об этом ниже). А считается она между положительными примерами, потому что только в них мы можем быть полностью уверены, негативные примеры мы можем выявить лишь косвено (попытки реализации подобного не увенчались успехом)

Я пробовал использовать контрастные лоссы, делать свой лосс с учетом и положительный и негативных примеров. Итог - модель теряла 50% метрики и почти сразу переобучалась.

На мой взгляд, это происходит, потому что сама задача состоит не в self-supervised обучении, при котором подобные функции потерь и используются, поэтому и работают они не так хорошо. Задача модели - лишь иммитировать эмбеддинги якорной модели, а для этого нужно жесткое соответствие с "истинными" эмбеддингами, поэтому и лучше работает MSE loss. Но в то же время, модель нужно штрафовать за слишком слабое "расталкивание" эмбеддингов.

**Вывод - требуется или пересмотр подхода, или поиск новой оптимальной функции потерь.**

---

### ⚙️ **Что использовал?**

- **ResNet18 (с весами ImageNet)** — для получения эмбеддингов изображений и спектрограмм аудио.  
- **Sentence Transformer — `thenlper/gte-base`** — в качестве поставщика текстовых эмбеддингов (выбран экспериментально среди нескольких кандидатов).  
- **MSELoss** - функция потерь.
- Вся архитектура реализована на PyTorch.

---

### 📈 **Что в будущем?**

1. Поиск эфективной функции потерь для улучшения результатов модели и повышения качества эмбеддингов.
2. Исследовать наиболее эффективные способы проекции (не ограничиваясь простой MLP моделью) и решить проблему «узкого горлышка» текстовой модели (об этом позже).  
3. Создание «Модели-гиганта» — модели, накопившей обширные знания о мире и способной генерировать очень ёмкие эмбеддинги для обучения, анализа и исследований.
---

### 🛑 **Риски и ограничения**

1. **«Узкое горлышко» — текстовая модель.** Если текстовое ядро слабое, результат будет неудовлетворительным. Возможное решение — дистилляция знаний и дообучение «ученика».  
2. **Проблемы ядра — общие проблемы.** Все ошибки и недопонимания текстовой модели будут воспроизводиться и усиливаться в других модальностях.  

---

### 🔚 **Заключение**

Этот проект создан в целях личного интереса и саморазвития. Я рад, что он подаёт признаки жизни, и надеюсь, смогу превратить его в нечто большее.

Связь со мной: emal - dmitry.kolesnik5894@ya.ru

Открыт к любым предложениям и критике :)